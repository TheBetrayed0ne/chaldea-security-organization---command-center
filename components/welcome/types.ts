
import { UserRole } from '../../context/StatusContext';

export interface RoleConfig {
  title: string;
  designation: string;
  color: string;
  icon: string;
}

export interface WelcomeTheme {
  primaryAccent: string;
  secondaryAccent: string;
  backgroundGlow: string;
}
